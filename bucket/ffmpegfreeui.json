{
    "version": "3.3",
    "description": "FFmpegFreeUI",
    "homepage": "https://github.com/Lake1059/FFmpegFreeUI/blob/main/README.md",
    "license": "MIT",
    "architecture": {
        "64bit": {
            "url": "https://github.com/Lake1059/FFmpegFreeUI/releases/download/3.3/FFmpegFreeUI.ReadyToRun.x64.7z",
            "hash": "9601ed2151a000bf31b6d254f8d20dd4302e86277c174630d4b6f19069b61b81"
        }
    },
    "extract_dir": "FFmpegFreeUI ReadyToRun x64",
    "shortcuts": [
        [
            "FFmpegFreeUI.exe",
            "FFmpegFreeUI"
        ]
    ],
    "pre_install": [
        "$settingsFile = Join-Path $persist_dir 'Settings.json'",
        "if (!(Test-Path $persist_dir)) { New-Item -ItemType Directory -Path $persist_dir | Out-Null }",
        "if (!(Test-Path $settingsFile)) { \"{}\" | Out-File -FilePath $settingsFile -Encoding utf8 }"
    ],
    "bin": "FFmpegFreeUI.exe",
    "persist": "Settings.json",
    "checkver": {
        "github": "https://github.com/Lake1059/FFmpegFreeUI",
        "regex": "/releases/tag/([\\d.]+)"
    },
    "autoupdate": {
        "architecture": {
            "64bit": {
                "url": "https://github.com/Lake1059/FFmpegFreeUI/releases/download/$version/FFmpegFreeUI.ReadyToRun.x64.7z"
            }
        }
    }
}
